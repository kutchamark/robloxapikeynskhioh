<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roblox Player Data</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
        }
        th, td {
            padding: 10px;
            text-align: left;
        }
    </style>
</head>
<body>
    <h1>Player Data</h1>
    <table>
        <thead>
            <tr>
                <th>Player Name</th>
                <th>Join Time</th>
                <th>Roblox User ID</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
            <tr>
                <td>{{ user.playername }}</td>
                <td>{{ user.join_time }}</td>
                <td>{{ user.robloxuserid }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
